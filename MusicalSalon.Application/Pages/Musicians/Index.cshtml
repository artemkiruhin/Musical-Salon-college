@page
@model MusicalSalon.Application.Pages.Musicians.IndexModel

<h2>Musicians</h2>

<p>
    <a asp-page="Add">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-page="Index" asp-route-sortOrder="id">@Html.DisplayNameFor(model => model.Musicians.First().Id)</a>
            </th>
            <th>
                <a asp-page="Index" asp-route-sortOrder="name">@Html.DisplayNameFor(model => model.Musicians.First().Name)</a>
            </th>
            <th>
                <a asp-page="Index" asp-route-sortOrder="careerYear">@Html.DisplayNameFor(model => model.Musicians.First().CareerYear)</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var musician in Model.Musicians)
        {
            <tr>
                <td>@musician.Id</td>
                <td>@musician.Name</td>
                <td>@musician.CareerYear</td>
                <td>
                    <a asp-page="Edit" asp-route-id="@musician.Id">Edit</a> |
                    <form method="post" asp-route-id="@musician.Id" asp-page-handler="Delete">
                        <button type="submit" class="text-danger">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
