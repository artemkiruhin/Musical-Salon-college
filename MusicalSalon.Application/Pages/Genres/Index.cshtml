@page
@model MusicalSalon.Application.Pages.Genres.IndexModel

<h2>Genres</h2>

<p>
    <a asp-page="Add">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-page="Index" asp-route-sortOrder="id">@Html.DisplayNameFor(model => model.Genres.First().Id)</a>
            </th>
            <th>
                <a asp-page="Index" asp-route-sortOrder="name">@Html.DisplayNameFor(model => model.Genres.First().Name)</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var genre in Model.Genres)
        {
            <tr>
                <td>@genre.Id</td>
                <td>@genre.Name</td>
                <td>
                    <a asp-page="Edit" asp-route-id="@genre.Id">Edit</a> |
                    <form method="post" asp-route-id="@genre.Id" asp-page-handler="Delete">
                        <button type="submit" class="text-danger">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
