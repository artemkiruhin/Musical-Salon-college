@page
@model MusicalSalon.Application.Pages.Providers.IndexModel

<h2>Поставщики</h2>

<p>
    <a asp-page="Add">Добавить</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-page="Index" asp-route-sortOrder="id">Id</a>
            </th>
            <th>
                <a asp-page="Index" asp-route-sortOrder="name">Название</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var provider in Model.Providers)
        {
            <tr>
                <td>@provider.Id</td>
                <td>@provider.Name</td>
                <td>
                    <a asp-page="Edit" asp-route-id="@provider.Id">Изменить</a> |
                    <form method="post" asp-route-id="@provider.Id" asp-page-handler="Delete">
                        <button type="submit" class="text-danger">Удалить</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
